change SDL2 include and lib folder location in makefile to the corresponding one in your computer.

If there is no window showing, copy compiled .exe file or binary file on the same directory as the folder "/imgui".
